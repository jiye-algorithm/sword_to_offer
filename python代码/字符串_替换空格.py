'''请实现一个函数，将一个字符串中的空格替换成“%20”。例如，当字符串为We Are Happy.则经过替换之后的字符串为We%20Are%20Happy。'''# -*- coding:utf-8 -*-class Solution:    # s 源字符串    def replaceSpace(self, s):        # write code here        return s.replace(" ", "%20")        passpassif __name__ == '__main__':    '''    测试用例：        * 输入字符串包含空格（三种情况，包括多个空格）            * 空格位于字符串最前面            * 空格位于字符串最后面            * 空额位于字符串中间            * 字符串中存在连续多个空格        * 输入字符串没有空格        * 特殊输入            * 字符串是个NULL指针            * 字符串是个空串            * 字符串只有一个空格字符            * 字符串只有连续多个空格    '''    str = "abc "    str2 = " abc"    str3 = "We Are Happy"    solution = Solution()    print(solution.replaceSpace(str))    print(solution.replaceSpace(str2))    print(solution.replaceSpace(str3))    pass'''注解：    如果合并两个数组中，从前往后每个数字重复移动多次，可以考虑从后往前复制'''