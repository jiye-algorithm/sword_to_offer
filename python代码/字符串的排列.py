'''输入一个字符串,按字典序打印出该字符串中字符的所有排列。例如输入字符串abc,则打印出由字符a,b,c所能排列出来的所有字符串abc,acb,bac,bca,cab和cba。'''# -*- coding:utf-8 -*-class Solution:    def __init__(self):        self.result = None        pass    def Permutation(self, ss):        if ss == None:            return        begin = 0        self._permutation(ss, begin)        return self.result        pass    def _permutation(self, str, begin):        if begin == len(str) - 1:            self.result.append(str)        else:            for index, ch in enumerate(str[begin:]):                begin, index = index, begin                self._permutation(str, begin + 1)                begin, index = index, begin        pass    pass